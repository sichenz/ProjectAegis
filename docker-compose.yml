services:
  edge-ai-node:
    build: .
    container_name: project-aegis-edge
    volumes:
      # Mount output directory to persist results on the host
      - ./output:/app/output
    # Default command runs evaluation. Override to run other scripts:
    # command: python scripts/visualize.py
    # command: python src/attack/fgsm_attack.py --model ensemble.pth
