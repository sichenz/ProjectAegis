version: '3.8'

services:
  edge-ai-node:
    build: .
    container_name: project-aegis-edge
    # We can mount volumes if we want to save logs or charts back to the host, 
    # but for a simple evaluation run, the default COPY in Dockerfile is fine.
    # To run the visualization script instead:
    # command: python scripts/visualize.py
